.text-center
  %h1 Sign up as host
  .mx-auto.bg-yellow-100.my-10.p-4.shadow-lg.text-xs.text-gray-800.tracking-wide{ class: 'w-full sm:w-1/2' }
    If you are a guest, you do not need to sign up.
    .mt-2
    Go to the
    = link_to 'home page', root_path, class: 'text-pink-500 hover:text-pink-600 transition duration-200 ease-in-out'
    and start over from there.

  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    .mb-8
      = f.label :name, class: 'text-gray-500 text-xs tracking-wide'
      = f.text_field :name, class: 'input-text'
      = render 'shared/forms/field_error_messages', form: resource, column: :name
    .my-8
      = f.label :email, class: 'text-gray-500 text-xs tracking-wide'
      = f.email_field :email, autofocus: true, autocomplete: "email", class: 'input-text'
      = render 'shared/forms/field_error_messages', form: resource, column: :email
    .my-8
      = f.label :password, class: 'text-gray-500 text-xs tracking-wide'
      = f.password_field :password, autocomplete: "new-password", class: 'input-text'
      = render 'shared/forms/field_error_messages', form: resource, column: :password
      - if @minimum_password_length
        .mt-1.text-xs.text-gray-400
          (#{@minimum_password_length} characters minimum)
    .actions.my-8
      = f.submit "Sign up", class: 'input-submit'
    .mt-10
      = render "devise/shared/links"
