- if visits.present? && visits.any?
  %ul
    - visits.each do |visit|
      %div{ data: { controller: 'synchronize-visit', 'synchronize-visit-token': visit.token } }
        = render 'list_item', visit: visit
- else
  .text-sm.text-gray-500 (none)
